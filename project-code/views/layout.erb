<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>CRuMbS: Costume Rental Management System</title>
    <link rel="stylesheet" href="/styles.css" type="text/css" />
  </head>
  <body>
    <div class="container" id="top">
      <div class="top-row">
        <div class="info-box">
        <div class="date"><%= DateTime.now.strftime("%d/%m/%Y %H:%M") %></div>
          <% if request.path_info == '/crms' %>
            <form method="post" action = "/crms/main">
              <label for = "password">Login: </label><br />
              <input class="textfield" type = "password" id="password" name="password" size="15" />
              <input type="submit" class="button" value="Main Menu" />
            </form>
            <% else %>
              <form method="get" action = "/crms">
                <input type="submit" class="blackbutton" value="Log Out" />
              </form>
            <% end %>
        </div>
        <div class="top-left">
        <div class="top-heading">
        <h1>CRuMbS: Costume Rental Management System</h1>
        </div>
      </div>
      <nav>
        <% if request.path_info != '/crms' %>
      <ul id="navbar">
        <li><a href="/crms/main">Home</a> </li>
        <li><a href="/crms/stock">Stock</a> </li>
        <li><a href="/crms/customers">Customers</a></li>
        <li><a href="/crms/rentals">Rentals</a></li>
      </ul>
      <% end %>
      </nav>
      </div>
      <% if request.path_info != '/crms' %>
<div class="main">
  <br class = "clear" />
  <% else %>
    <div>
    <% end %>
    <%= yield %>
  </div> <!-- end main -->

  <footer><div class="bottom center"><em>CRuMbS: &copy; 2019, A. Treshansky</em></div></footer>

  </div> <!-- end container -->
  </body>
</html>
